<UserControl x:Class="Projekat18.View.AddTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projekat18.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="White" CornerRadius="8" BorderBrush="#DDD" BorderThickness="1" Padding="12" Margin="16">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical" Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Center">
                <TextBlock Text="Ime tabele:" FontWeight="SemiBold"/>
                <TextBox Text="{Binding TableName, UpdateSourceTrigger=PropertyChanged}" MinWidth="120" Height="28"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Grid.Column="1" Margin="0,0,12,0" VerticalAlignment="Center">
                <TextBlock Text="Kolone (zarezom odvojene):" FontWeight="SemiBold"/>
                <TextBox Text="{Binding ColumnsText, UpdateSourceTrigger=PropertyChanged}" MinWidth="180" Height="28"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Dodaj" Command="{Binding AddCommand}" Width="110" Margin="0,0,12,0"/>
                <Button Content="Otkaži" Command="{Binding CancelCommand}" Width="80"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
